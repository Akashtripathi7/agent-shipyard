# <Project name>

<!-- Only what an agent can't work out from the code. Add one rule at a time; audit monthly. -->

## Commands
- All checks: scripts/check.sh (run before saying any task is done)
- <Other non-obvious commands, e.g. how to run one test, seed data, start the app>

## Working conventions
- Test first: write a failing test, run it, see it fail, then implement.
- Never report something as working without running it. Show the evidence.
- Never change what an existing test expects without asking me.
- Don't commit or push unless I ask.

## Product rules
- <Decisions the code can't reveal, e.g. "amounts are integer paise, never floats">

## Outside this repo
- <Pointers: other repos, where staging lives, who owns what>
